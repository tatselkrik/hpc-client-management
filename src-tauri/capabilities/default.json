{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Allows save dialogs and write access only to common user export locations.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-save",
    "fs:allow-desktop-write-recursive",
    "fs:allow-document-write-recursive",
    "fs:allow-download-write-recursive"
  ]
}